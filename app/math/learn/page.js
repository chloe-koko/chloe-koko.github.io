import LearnClient from './LearnClient';

export const metadata = {
  title: "Prime Numbers — Chloe's Site",
  description: "Discover prime numbers through the Sieve of Eratosthenes — interactive story and exercises for 6th graders.",
};

export default function LearnPage() {
  return <LearnClient />;
}
